#!/usr/bin/env python
#coding=utf-8
import web
import yunyin

#获取当前的用户名
def getName():
	# name=web.session(name=False)
	if not name:
		token=web.cookies().get(token=False)
	return False
#获取当前用户的id
def getId():
	return 0

#获取电话
def getPhone():
	return False

